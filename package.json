{
  "name": "madeineurope.dev",
  "version": "1.0.0",
  "private": true,
  "description": "A curated directory of developer tools, software & hardware made in Europe. Static site generator + dynamic front-end driven by declarative JSON data.",
  "type": "module",
  "scripts": {
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\"",
    "build": "node scripts/build.mjs",
    "dev": "node --watch --watch-path=data --watch-path=src --watch-path=scripts scripts/build.mjs & node scripts/serve.mjs dist 4321",
    "serve": "node scripts/serve.mjs dist 4321",
    "start": "npm run build && npm run serve"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
